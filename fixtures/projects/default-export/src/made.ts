/** The declaration the default export of this module calls. */
function make(): number {
  return 1;
}

/** A default export of a call, which names nothing declared elsewhere. */
export default make();
