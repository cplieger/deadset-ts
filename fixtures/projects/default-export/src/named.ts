/** The declaration the default export of this module names. */
export function named(): number {
  return 2;
}

/** A default export of a name, which is a link to the declaration above. */
export default named;
